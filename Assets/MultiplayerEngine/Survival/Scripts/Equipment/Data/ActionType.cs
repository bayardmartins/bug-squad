namespace Ignitives.MultiplayerEngine
{
    // ItemType enum removed — animator now uses ItemID (unique per item) instead of ItemType + ActionID.
    // This file is kept for compatibility but the enum is no longer needed.
}
